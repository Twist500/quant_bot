{
  "version": 2,
  "builds": [
    { "src": "api/*.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/callback", "dest": "/api/callback" },
    { "src": "/callback/(.*)", "dest": "/api/callback" },
    { "src": "/(.*)", "dest": "/api/callback" }
  ]
}
